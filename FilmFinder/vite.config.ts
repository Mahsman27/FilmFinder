import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    port: 4000,
  },
  build: {
    // define your build options here
  },
  plugins: [
    // define plugins here..      
  ],
});